---
// Live chat widget with WhatsApp integration for immediate interaction
---

<!-- Live Chat Widget -->
<div class="fixed bottom-4 left-4 z-40" id="chat-widget">
  <!-- Chat Toggle Button -->
  <button
    id="chat-toggle"
    class="w-14 h-14 bg-green-600 hover:bg-green-700 text-white rounded-full shadow-2xl flex items-center justify-center transition-all duration-300 touch-manipulation hover:scale-110"
    aria-label="Open live chat"
  >
    <svg id="chat-icon" class="w-7 h-7" fill="currentColor" viewBox="0 0 24 24">
      <path d="M20 2H4c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h4l4 4 4-4h4c1.1 0 2-.9 2-2V4c0-1.1-.9-2-2-2zm-2 12H6v-2h12v2zm0-3H6V9h12v2zm0-3H6V6h12v2z"/>
    </svg>
    <svg id="close-chat-icon" class="w-6 h-6 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
    </svg>
  </button>

  <!-- Chat Panel -->
  <div
    id="chat-panel"
    class="absolute bottom-16 left-0 w-80 max-w-[calc(100vw-2rem)] bg-white rounded-2xl shadow-2xl border border-stone-200 opacity-0 invisible transform scale-95 transition-all duration-300 origin-bottom-left"
  >
    <!-- Chat Header -->
    <div class="bg-green-600 text-white p-4 rounded-t-2xl">
      <div class="flex items-center gap-3">
        <div class="w-10 h-10 bg-white/20 rounded-full flex items-center justify-center">
          <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
            <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893A11.821 11.821 0 0020.893 3.690"/>
          </svg>
        </div>
        <div>
          <h3 class="font-semibold">Need Help?</h3>
          <p class="text-sm text-green-100">Chat with us on WhatsApp</p>
        </div>
        <div class="ml-auto">
          <div class="w-2 h-2 bg-green-300 rounded-full animate-pulse"></div>
        </div>
      </div>
    </div>

    <!-- Chat Options -->
    <div class="p-4 space-y-3">
      <!-- Emergency Option -->
      <a
        href="https://wa.me/16722003045?text=🚨%20EMERGENCY%20-%20I%20need%20immediate%20plumbing%20or%20electrical%20help!%20Please%20call%20me%20right%20away."
        target="_blank"
        rel="noopener noreferrer"
        class="block w-full bg-red-600 hover:bg-red-700 text-white p-3 rounded-xl transition-colors duration-200 touch-manipulation"
      >
        <div class="flex items-center gap-3">
          <div class="w-8 h-8 bg-white/20 rounded-lg flex items-center justify-center">
            <span class="text-lg">🚨</span>
          </div>
          <div class="text-left">
            <div class="font-semibold text-sm">Emergency Help</div>
            <div class="text-xs text-red-100">Immediate response needed</div>
          </div>
        </div>
      </a>

      <!-- General Inquiry -->
      <a
        href="https://wa.me/16722003045?text=Hi!%20I%20have%20a%20question%20about%20your%20plumbing%20and%20electrical%20services.%20Can%20you%20help%20me?"
        target="_blank"
        rel="noopener noreferrer"
        class="block w-full bg-blue-600 hover:bg-blue-700 text-white p-3 rounded-xl transition-colors duration-200 touch-manipulation"
      >
        <div class="flex items-center gap-3">
          <div class="w-8 h-8 bg-white/20 rounded-lg flex items-center justify-center">
            <span class="text-lg">💬</span>
          </div>
          <div class="text-left">
            <div class="font-semibold text-sm">General Question</div>
            <div class="text-xs text-blue-100">Ask about our services</div>
          </div>
        </div>
      </a>

      <!-- Quote Request -->
      <a
        href="https://wa.me/16722003045?text=Hi!%20I'd%20like%20to%20request%20a%20free%20quote%20for%20plumbing/electrical%20work.%20Here%20are%20the%20details:%20"
        target="_blank"
        rel="noopener noreferrer"
        class="block w-full bg-green-600 hover:bg-green-700 text-white p-3 rounded-xl transition-colors duration-200 touch-manipulation"
      >
        <div class="flex items-center gap-3">
          <div class="w-8 h-8 bg-white/20 rounded-lg flex items-center justify-center">
            <span class="text-lg">💰</span>
          </div>
          <div class="text-left">
            <div class="font-semibold text-sm">Get Free Quote</div>
            <div class="text-xs text-green-100">Fast estimate via chat</div>
          </div>
        </div>
      </a>

      <!-- Service Booking -->
      <a
        href="https://wa.me/16722003045?text=Hi!%20I'd%20like%20to%20book%20a%20service%20appointment.%20Please%20let%20me%20know%20your%20availability."
        target="_blank"
        rel="noopener noreferrer"
        class="block w-full bg-slate-700 hover:bg-slate-600 text-white p-3 rounded-xl transition-colors duration-200 touch-manipulation"
      >
        <div class="flex items-center gap-3">
          <div class="w-8 h-8 bg-white/20 rounded-lg flex items-center justify-center">
            <span class="text-lg">📅</span>
          </div>
          <div class="text-left">
            <div class="font-semibold text-sm">Book Service</div>
            <div class="text-xs text-slate-300">Schedule an appointment</div>
          </div>
        </div>
      </a>
    </div>

    <!-- Alternative Contact -->
    <div class="px-4 pb-4 border-t border-stone-100 pt-3">
      <p class="text-xs text-stone-600 text-center mb-2">Or call us directly:</p>
      <a
        href="tel:6722003045"
        class="block text-center text-sm font-semibold text-blue-600 hover:text-blue-700 transition-colors touch-manipulation"
      >
        (672) 200-3045
      </a>
    </div>
  </div>

  <!-- Notification Badge -->
  <div
    id="chat-notification"
    class="absolute -top-1 -right-1 w-5 h-5 bg-red-500 text-white text-xs rounded-full flex items-center justify-center font-bold animate-bounce"
  >
    !
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const chatToggle = document.getElementById('chat-toggle');
    const chatPanel = document.getElementById('chat-panel');
    const chatIcon = document.getElementById('chat-icon');
    const closeChatIcon = document.getElementById('close-chat-icon');
    const notification = document.getElementById('chat-notification');

    let isOpen = false;

    function openChat() {
      chatPanel?.classList.remove('opacity-0', 'invisible', 'scale-95');
      chatIcon?.classList.add('hidden');
      closeChatIcon?.classList.remove('hidden');
      notification?.classList.add('hidden');
      isOpen = true;
    }

    function closeChat() {
      chatPanel?.classList.add('opacity-0', 'invisible', 'scale-95');
      chatIcon?.classList.remove('hidden');
      closeChatIcon?.classList.add('hidden');
      isOpen = false;
    }

    function toggleChat() {
      if (isOpen) {
        closeChat();
      } else {
        openChat();
      }
    }

    // Toggle chat on button click
    chatToggle?.addEventListener('click', toggleChat);

    // Close chat when clicking outside
    document.addEventListener('click', function(e) {
      const chatWidget = document.getElementById('chat-widget');
      if (!chatWidget?.contains(e.target as Node) && isOpen) {
        closeChat();
      }
    });

    // Hide notification after 10 seconds
    setTimeout(() => {
      notification?.classList.add('hidden');
    }, 10000);

    // Show notification periodically if chat hasn't been opened
    let notificationInterval = setInterval(() => {
      if (!isOpen && notification) {
        notification.classList.remove('hidden');
        setTimeout(() => {
          notification?.classList.add('hidden');
        }, 5000);
      }
    }, 30000); // Every 30 seconds

    // Track when WhatsApp links are clicked
    const whatsappLinks = chatPanel?.querySelectorAll('a[href*="wa.me"]');
    whatsappLinks?.forEach(link => {
      link.addEventListener('click', function() {
        closeChat();
        clearInterval(notificationInterval);
      });
    });

    // Hide chat widget in footer area to avoid conflicts
    function handleScroll() {
      const footer = document.querySelector('footer');
      const chatWidget = document.getElementById('chat-widget');

      if (footer && chatWidget) {
        const footerRect = footer.getBoundingClientRect();
        const windowHeight = window.innerHeight;

        if (footerRect.top < windowHeight + 100) {
          chatWidget.style.transform = 'translateY(20px)';
          chatWidget.style.opacity = '0.7';
        } else {
          chatWidget.style.transform = 'translateY(0)';
          chatWidget.style.opacity = '1';
        }
      }
    }

    let scrollTimeout: NodeJS.Timeout;
    window.addEventListener('scroll', () => {
      clearTimeout(scrollTimeout);
      scrollTimeout = setTimeout(handleScroll, 16);
    });

    handleScroll();
  });
</script>